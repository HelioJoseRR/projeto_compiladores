# Example 1: Demonstração de tipos básicos e aritmética

/* Este exemplo demonstra:
   - Declaração de variáveis com tipo number
   - Operações aritméticas básicas
   - Uso de números inteiros e reais
*/

number inteiro = 10;
number real = 3.14;
number soma = inteiro + real;
number produto = inteiro * 2;
number divisao = real / 2;
number modulo = inteiro % 3;

# Operações compostas
number resultado = (inteiro + real) * produto - divisao;
